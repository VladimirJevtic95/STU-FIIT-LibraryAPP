
read me

To import the database you need to chage the input file size limit (in XAMPP on apache clikc config -> file to do that is-> 
(PHP.ini ->post_max_filesize nad ->upload_max_filesize)). 
DATABASE SIZE IS 148MB. 
and also you need to disable or make longer the execution time limit. i recommend setting the walue to 0